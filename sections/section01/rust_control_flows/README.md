# rust_control_flows

Examples of core Rust control flow patterns.

## What this project covers

- `if` / `else if` / `else`
- Using `if` as an expression
- `loop` with `break` returning a value
- `while` loops
- `for` loops over ranges and collections

## Run

```bash
cargo run
```

## Build

```bash
cargo build
```
